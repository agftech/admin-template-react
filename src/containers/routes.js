import Cards from './../components/inside/Cards';

const LoadingRoutes  = () => ([
    {
        path: "/cards",
        name: "Cards",
        template: Cards
    }
]);

export default LoadingRoutes();